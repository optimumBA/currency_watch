{
  "addons": ["heroku-postgresql:hobby-dev"],
  "env": {
    "HEROKU_APP_NAME": {
      "required": true
    },
    "SECRET_KEY_BASE": {
      "generator": "secret"
    }
  },
  "scripts": {
    "postdeploy": "bin/currency_watch eval 'CurrencyWatch.Release.migrate' && bin/currency_watch eval 'CurrencyWatch.Release.seed'"
  },
  "stack": "container"
}
